{% extends 'base.html.j2' %}

{% block body %}

<div class="mb-2">
    {% if not_found %}
        <p>Продукты, которые мы не нашли в нашем справочнике:<br><br>
        {{ ', '.join(not_found) }} 
        </p>
    {% endif %}


    {% if not nutrient_resource %}
        <p>Ура! Вы получили все необходимые нутриенты!<br>
        Так держать! :)
        </p>
        {% else %}

        <p>Следующих нутриентов может не хватать в рационе:<br>
            <ul>
            {% for k, v in nutrient_resource.items() %}
            <li> {{ k }}: {{ nutrient_dict[k] }}/{{ recommend_amount[k] }}</li>
            {% endfor %}
            </ul>
        </p>

        <p> Калории: {{ kkal }}/1567 <br>
        </p>

        <p>Ниже указаны источники, из которых их можно получить.
            <ul>
            {% for k, v  in nutrient_resource.items() %}
            <li> {{ k }}: {{ ', '.join(v) }} </li>
            {% endfor %}
            </ul>
        </p>
    {% endif %}
</div>

{% endblock %}
